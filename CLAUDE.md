# CLAUDE.md — 앱5 — 살면서 악기 하나쯤은 다룰 줄 알아야지

> 이 repo 전용 상세 문서. 공통 규약·다른 앱 맥락·이 PC 로컬 검증 함정은 상위 `AI/CLAUDE.md` 에 있다.
> 지난 작업 이력은 `AI/docs/history/` 월별 파일 참고.
> Codex(GPT)도 이 파일을 읽는다 — `~/.codex/config.toml` 의 project_doc_fallback_filenames 설정.

### instrument-quiz (앱5) — 살면서 악기 하나쯤은 다룰 줄 알아야지
- **라이브: https://instrumentquiz.org** (+ 기존 `instrument-quiz.hspatrick.workers.dev` 도 유효). ⚠️ **www 서브도메인은 미등록**(추가 권장). ⚠️ **이 PC(중국망)에서는 도메인 DNS가 차단돼 안 열림** — 사이트 문제 아님. 원인 진단 결과: 일반 DNS(114.114.114.114·8.8.8.8·1.1.1.1)는 이 도메인만 NXDOMAIN, **DoH(암호화 DNS)로는 정상 응답**, IP 직접 접속도 200. 해결 = 브라우저 "보안 DNS(Cloudflare)" 켜기 또는 hosts에 `104.21.89.220 instrumentquiz.org`. 폰·해외는 정상.
- **repo**: `ppiol8230-glitch/instrument-quiz` (my-site `.gitignore`에 제외됨). 배포 = git push → Workers Builds 자동.
- **파일 구조**:
  - `public/index.html` — 앱 전체(질문 정의·게이트/스코어링 엔진·Web Audio 테스트·결과 렌더·공유 카드 Canvas). IIFE 클로저, **`window.APP`만 전역**.
  - `public/data/inst_*.js` × 9 — 악기 **200종** DB(건반·프리리드21 / 활현악11 / 민속현악30 / 기타·만돌린20 / 하프·류트24 / 서양목관17 / 민속목관23 / 금관·유율타악23 / 무율타악·힐링·전자31). 스키마는 repo 루트 `SCHEMA.md`.
  - `public/data/genres.js` — canonical 장르 26종(**악기 DB의 genres는 반드시 이 문자열만**). `public/data/rarity.js` — 랜덤 프로필 6,000명을 엔진에 통과시켜 얻은 1위 분포(희귀도 표시용).
  - `public/art/art_*.js` × 9 — 악기별 SVG 일러스트 189종(viewBox 0 0 100 100, 실물 시그니처 반영). 결과 카드·배경 낙서에 사용.
  - `public/guide/guide_*.js` × 9 — 악기별 입문 가이드 189종(`how`/`lesson`/`buy`/`firstGoal`, 실재 브랜드·한국 시세 웹검증).
  - `public/og/*.png` — 악기별 링크 미리보기 1200×630 (PowerShell GDI+로 생성, 재생성 스크립트는 대화 이력 참고). `public/og.png` = 기본 미리보기.
  - `src/index.js` + `src/names.js` — Worker. `/r/<악기id>` 요청이면 index.html을 읽어 title·og:* 를 그 악기용으로 치환. 그 외는 `env.ASSETS.fetch` 통과.
  - `QUESTIONS.md` — 문항 은행(설계 문서). `test.html` — DB 무결성 검증 페이지(중복 id·필수필드·장르 위반 카운트).
- **엔진 요약**: `SLOTS`(악기×트랙) → `scoreSlot()` = 하드 게이트 → 청음(pitchFixed F0/F1/F2/F3/P × 청음점수 밴드, 가중 최대) → 리듬 → 장르(직접 F1~F3 + 간접 E축) → 역할(복수) → 음색 → 희귀/유행/즉흥/무대 → 인내vs즉각보상 → 학습환경 → 목표·합주 → 노래반주 → 경험 친족(kinship) → 신체·통증. `NOVELTY`(카주·구금 등 장난악기 1위 감쇠), `INSTITUTIONAL`(파이프오르간·피아노·드럼·팀파니·마림바·콘서트하프·튜바·더블베이스 등 15종은 **학원/교회/연습실 접근 악기라 예산 게이트 면제**).
- **결과 3장**: ①가장 어울리는 악기 ②`accessInfo()` 통과(레슨 흔함 or 독학 자료 많음 + 진입장벽≤3 + 입문가≤70만 + 희귀도≤3) 악기 = "🌱 좀 더 쉽게 시작하고 싶다면" ③"👀 이런 악기도 있어요"(다른 계열 우선).
- **공유 링크**: `packAnswers()`가 질문 순서·선택지 개수가 고정인 점을 이용해 **질문마다 최소 비트만** 기록(조건부 스킵 질문은 비트 미소비, 점수는 앞머리에 7비트). 결과 = `/r/<악기id>#<22자>`. 구형 `#s=`(JSON)·`#r=`(글자단위) 링크도 계속 디코드.
- **로컬 검증 방법**: PowerShell HttpListener를 `run_in_background`로 인라인 실행하되 **`public/`를 루트로 매핑**(`/data/...` 절대경로 때문에 필수), `/r/*`는 index.html로 폴백. 브라우저는 `preview_start` → `javascript_tool`로 `APP.*` 호출. 카드 이미지 눈으로 확인하려면 서버에 POST 핸들러를 붙여 canvas blob을 파일로 받아 `Read`. **디버그 훅**: `APP._cov(n)`(1위+후보·와일드카드 커버리지 집계 — 질문 개편 시 "누락 악기 0" 검증용)·`APP._mc(n)`·`APP._auto()`·`APP._build()`·`APP._score(slot)`·`APP._profile()`·`APP._slots`·`APP._lang()`·`APP.setLang(l)`.
- **✅ 3종 수정(2026-08-02, 커밋 `9bf6f62`·라이브)**: ①**장르 미스매치 감점**(직접 고른 장르와 안 겹치면 −4×고른수(≤3), 민속 전용 악기는 −8 추가)+**민속 보너스 게이트**(민속 장르 실제 선택 시만 온전히) — "팝·가요 골랐는데 아프리카 악기" 해결, rarity.js 2만 명 재생성. ②**공유 카드 fitFont**(글자수 절단 폐지→폭 맞춤 자동 축소)+wrapText 중국어 글자 단위 줄바꿈. ③**결과 URL 영속화** — 결과 표시 시 `replaceState`로 `/r/<id>[?l=]#4.<pack>`+sessionStorage `iq_own` → 재로드 시 주인 화면 복원(공유 배너는 남의 링크만). ⚠️ `unpackAnswers`가 P.trail 재구성(복원 후 재공유 링크 오염 방지). zh 小红书 웹 폴백은 새 탭. 상세는 헤더 ㊅.
- **✅ 중국어판 + 중국 전통악기 11종 = 200종(2026-07-31, 커밋 `5be9d56`·라이브)**: 상세는 문서 상단 헤더 ㊆. **구조 요점**: zh는 en과 같은 표시 오버레이(`public/i18n/zh_*.js` 9배치 + `zh_misc.js`) + **UI만 사전 방식**(`zh_ui.js`의 `ZH_UI` — 한국어 원문 키를 공백 정규화(`ZHN`)해 조회, 보간 문장 38곳만 L() 3번째 인자). 언어 우선순위: 저장선택 > `?l=` > trace 국가(**KR→ko·CN/HK/MO/TW/SG→zh·그 외→en**) > 브라우저 언어. 공유 링크 `?l=zh`→워커가 `src/names_zh.js`로 중문 OG + `og/zh/<id>.png`(중문 카드 200장). zh에선 유튜브 대신 **小红书 버튼**(`APP.vidOpen`: xhsdiscover 앱 스킴→웹 폴백, 위챗은 브라우저 열기 안내 `showWxTip`; 검색어 `ZH_VID` 오버라이드+기본 규칙(zh 이름 괄호·부제 제거)+" 演奏"). **가격은 언어별 통화**(zh ¥·en $·ko 원): 코드는 `usd()/cny()`(환율 2026-07 고정), en/zh 가이드·본문 프로즈는 일괄 환산 완료 — **환율 갱신하려면 KRW_PER_USD/KRW_PER_CNY 상수 + 데이터 파일 재환산 필요**(스크립트 방식은 git log `57fe5c8` 참고). 신규 악기 id: yangqin·zhongruan·liuqin·sanxian·konghou·jinghu(민속현악) / suona·bawu·xun(민속목관) / sheng(건반·프리리드) / muyu(무율타악·힐링). ⚠️ 새 악기 추가 시 체크리스트 = inst+art+guide+en+zh 오버레이+names 3종(src/)+og ko/zh 카드+rarity 재생성+index.html "200종" 문구. ⚠️ OG 카드 재생성 스크립트는 세션 scratchpad `gen_og.ps1`(GDI+·UTF-8 BOM 필수) — 새로 만들려면 대화 이력 참고.
- **✅ 설문 v4 압축(2026-07-27, 커밋 `73a5164`)**: 유저 "리듬 테스트 이후 질문이 너무 많다" → 리듬 테스트 이후 30→**24문항**(전체 ~28). 스코어링 기여를 전수 추적해 **알고리즘 무손실만** 제거: 죽은 변수 3개(`care`·`lefty`·`explore` — 엔진이 읽지 않음) 삭제 / 미미·중복 5개(D6 하루시간·E13 반복작업·E10 주말충전·C9 운동·E4 스마트폰 → gear축은 D2 "전자가 취향" 가중을 +0.5→+1로 올려 흡수) 삭제 / **병합 4쌍**(C2 손직업+C3 계단+C4 교정 → C1 체크리스트 흡수, C5 손크기+C8 손톱 → 손 1문항, A5 악보+A6 코드 → 1문항, E5 옷+E12 골동품 → "물건 고를 때" 1문항) / D4·D7은 스코어링 동일값 선택지 통합. **기존 버그 2건 수정**: ⓐ테스트를 건너뛴 유저는 **A7 답변 순간 `when(pitch===null)`이 자기 답변으로 꺼져 인덱스가 밀리며 C1(몸 게이트!)이 통째로 건너뛰어지던** 것 → `P.skipT` 플래그로 판정 ⓑD5 레슨 게이트 `lessonEnv===2`→`<=2`("온라인뿐"이 게이트를 안 걸던 것). 검증: 랜덤 2만 + **악기별 맞춤 프로필 189종 전수** → **추천 노출 상실 0·순위 하락 0**, 1위 도달 커버리지 155→164종. ⚠️ 질문 배치가 바뀌어 **공유 링크에 `#4.` 버전 접두사 도입**(구형 1~3세대 링크는 오해독 방지로 만료·인트로 폴백), `rarity.js`도 2만 명으로 재생성.
- **✅ 영어판 추가 — 한/영 2개 언어(2026-07-27, 커밋 `87dfa0a`)**: 유저 요청 "언어 버튼으로 영어, 번역 완벽하게, 악기 스펠링 정확히 교차검증, 어색하지 않게, 친절한 말투". **⭐ 핵심 설계 = 표시 오버레이**: `public/i18n/en_*.js` 9배치 + `en_misc.js`(장르 26·음색 36·역할 5 라벨). **스코어링 엔진은 언제나 한국어 원본 필드만 읽는다** — `family`·`name`·`roles`·`sound.tone`·`genres`·`ensemble`·`handSize` 매칭이 전부 한국어 정규식이기 때문. 영어는 **표시 직전에만** 접근자로 덧씌운다(`iName`/`iFam`/`iToneDesc`/`iCaveats`/`iTrackLabel`/`iTrackNote`/`iGuide`/`dName`, 라벨은 `gLabel`/`toneLabel`/`roleLabel`). ⚠️ **오버레이로 `inst.name`·`family`를 절대 덮어쓰지 말 것** — 덮으면 위 정규식이 전부 깨진다. 이 설계 덕에 **같은 답변이면 두 언어 추천 결과가 완전 동일**(랜덤 500 프로필 대조, 불일치 0). 오버레이 누락 항목은 한국어로 자연 폴백. **번역**: 9배치 병렬 에이전트로 189종 × (name·family·toneDesc·caveats·tracks·variants + 가이드 4필드). 악기 영문명은 각 항목의 `aka`를 1차 근거로 쓰되 표준 표기로 교차검증(심로→**Shimro**·Nyckelharpa·Viola d'amore·Great Highland bagpipe·Hurdy-gurdy·Saz (bağlama) 등), 배치 간 대소문자도 sentence case 통일. UI 전면 영역화(인트로·28문항·귀 테스트 5종·**절대음감 건반은 C·D·E 표기**·결과·페르소나 12종·레이더(라벨 짧게: Abs. pitch/Uniqueness/Grit)·희귀도·궁합·와일드카드·아차상·공유카드 canvas·모달). 가격은 `₩150K–400K` 형식, 한국 브랜드·상점은 로마자+짧은 설명 병기(Nakwon Arcade, Danggeun Market 등). **언어 전환**: 우상단 토글(`APP.setLang`), 진행 중 바꿔도 답변(P) 유지하고 **현재 화면만 다시 그림**(`VIEW`에 현재 렌더 함수 보관). 첫 진입 = 저장선택(localStorage `iq_lang`) > `?l=` > **Cloudflare `/cdn-cgi/trace` 접속국가(KR만 ko, 그 외 en)** > 브라우저 언어. 공유 링크는 영어일 때 `?l=en`이 붙고 **워커가 영어 OG 제목·설명으로 응답**(`src/names_en.js` 신설 — 해시 뒤 결과 데이터는 서버로 안 가므로 언어만 쿼리로 전달). **부수 수정**: 타블라 가이드 티탈 8박→**16박**(teental은 16박자, 번역 에이전트가 발견). 검증: 오버레이 커버리지 189/189·en 완주 한국어 누출 0·ko 회귀·모바일 가로넘침 0·라이브 OG 한/영 확인·콘솔 0.
- ⚠️ **위 항목 중 stale 주의**: 악기 수는 이제 **200종**(2026-07-31 중국 악기 11종 추가 — 위 구조 설명의 옛 카운트 189·art/guide/og 189장 표기는 전부 200 기준으로 읽을 것, og는 ko 200장+`og/zh/` 중문 200장). 언어는 **ko/en/zh 3개**(`i18n/en_*`+`i18n/zh_*`+`zh_ui.js`+`zh_misc.js`, 워커 names.js/names_en.js/names_zh.js). `rarity.js`는 **2만 명·200종** 기준, 공유 링크는 `/r/<악기id>[?l=en|zh]#4.<압축>` 이고 **구형 `#s=`·`#r=`·무접두사 링크는 더 이상 디코드하지 않는다**(질문 배치가 달라져 엉뚱한 결과가 나오므로 인트로로 폴백).

